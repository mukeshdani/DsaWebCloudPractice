
const initialState = {
    cardData:[]
} 

export default function cardItem(initialState, action){
   switch(action.type){
    case "ADD_TO_CARD"
        return {
            ...staticRange
        }
   }
}